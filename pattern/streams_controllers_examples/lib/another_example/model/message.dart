import 'package:flutter/material.dart';

class Message {
  final String sender;
  final String messageContent;
  final Color photoColor;

  Message({this.sender, this.messageContent, this.photoColor});
}
